services:
  postgres:
    image: postgres:16-alpine
    profiles: [local]
    environment:
      POSTGRES_USER: diabeo
      POSTGRES_PASSWORD: password
      POSTGRES_DB: diabeo
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
